{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.8",
    "daisyui": "^5.0.43",
    "tailwindcss": "^4.1.8"
  },
  "scripts": {
    "build": "npx @tailwindcss/cli -i app.css -o build.css",
    "watch": "npx @tailwindcss/cli -i app.css -o build.css --watch"
  }
}
